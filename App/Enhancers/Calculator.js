const multiply = (x) => (y) => {
  return x * y;
};

const devide = (x) => (y) => {
  return x / y;
};

const add = (x) => (y) => {
  return x + y;
};

const substract = (x) => (y) => {
  return x - y;
};

export {
  multiply,
  devide,
  add,
  substract,
};
